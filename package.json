{
  "name": "gramlisting",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "static_url": "export https_proxy='' http_proxy='' all_proxy='' &&  ngrok http --domain=witty-thankful-swine.ngrok-free.app https://localhost:3000"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ]
}
